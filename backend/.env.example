# Atlas MVP Configuration
# 本项目使用本地 MLX 模型，无需外部 API 服务
# 复制此文件为 .env 并根据需要修改配置

# ===========================================
# 模型配置 (可选覆盖 config.py 默认值)
# ===========================================
# LLM 模型 - 用于对话和文档分析
# LLM_MODEL_ID=mlx-community/Qwen3-4B-Instruct-2507-4bit

# 嵌入模型 - 用于文档向量化
# EMBEDDING_MODEL_ID=mlx-community/Qwen3-Embedding-0.6B-4bit-DWQ

# 视觉模型 - 用于 PDF 图像解析
# VISION_MODEL_ID=tencent/HunyuanOCR
# VISION_MODEL_BACKUP_ID=lmstudio-community/Qwen3-VL-4B-Instruct-MLX-4bit

# 重排序模型 - 用于搜索结果优化
# RERANK_MODEL_ID=mlx-community/Qwen3-Reranker-0.6B-4bit

# ===========================================
# 服务器配置
# ===========================================
HOST=0.0.0.0
PORT=8000
DEBUG=true

# ===========================================
# 数据库配置
# ===========================================
DATABASE_URL=sqlite+aiosqlite:///./atlas.db

# ===========================================
# 向量存储配置
# ===========================================
CHROMA_PERSIST_DIR=./chroma_db

# ===========================================
# 文档处理配置
# ===========================================
# 文档分块大小（字符数）
CHUNK_SIZE=800
# 分块重叠大小
CHUNK_OVERLAP=150
# 最大上传文件大小（字节，默认 50MB）
MAX_UPLOAD_SIZE=52428800

# ===========================================
# 搜索配置
# ===========================================
# 初始检索数量
TOP_K_RETRIEVAL=20
# 重排序后返回数量
TOP_K_RERANK=5
# BM25 权重（混合搜索）
BM25_WEIGHT=0.3
# 向量搜索权重
VECTOR_WEIGHT=0.7
